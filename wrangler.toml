name = "appealmate-cf"
pages_build_output_dir = "_cf_build"

[vars]
ADMIN_PASS = "BEN10owa"

# Analytics Engine binding (for event tracking - user clicks, letters generated, conversions)
# Dataset created in Cloudflare Dashboard - events flow in automatically
[[analytics_engine_datasets]]
binding = "ANALYTICS_ENGINE"
